/**
 * Generates a personalized greeting message.
 * @param name - The name of the person to greet
 * @returns A greeting message string
 */
export function greet(name: string): string {
  return `Hello, ${name}! Welcome to Leonidas.`;
}
